import { Typography } from '@mui/material';
import React from 'react';

const AllProducts = () => {
    return (
       <Typography>OUR ALL PRODUCTS</Typography>
    );
};

export default AllProducts;